# Version Log

记录已经落地的业务能力、配置语义与工程规则变更。同一自然日的后续变更追加到现有日期块；纯格式调整不记录。

## 2026-07-14 — v0.1.0 基线

- 建立飞书长连接机器人，可接收聊天中直接上传的 `.xlsx`、`.xls`、`.xlsm` 附件。
- 通过 IM 消息资源接口获取文件，以大小限制、文件名清理、`.part` 原子落盘和重复消息幂等策略保存至 `data/inbox/`。
- 建立离线 pytest，覆盖配置、鉴权下载、附件解析和幂等行为。
- 建立 Cursor 协作规则、架构地图、目录职责说明和功能计划模板。

### 表格链接导出

- 支持 `*.feishu.cn/sheets/<token>` 直接导出 XLSX，并支持 `*.feishu.cn/wiki/<token>` 先解析 Wiki 节点再导出。
- Wiki 仅接受真实对象类型为 `sheet` 或 `bitable`；使用节点返回的 `obj_token` 创建导出任务，不把 Wiki Token 当作文档 Token。
- 新增导出任务创建、轮询和原子下载客户端能力；链接导出归档至 `data/archive/YYYY-MM/sender_open_id/`，Wiki 标题优先。
- 新增 `FEISHU_ARCHIVE_DIR` 配置、归档产物 Git 忽略规则、Wiki 权限不足的明确机器人提示，以及对应的 API/解析/监听器离线测试。
- 文档补充 Wiki 和云文档导出权限、开放平台 Scope 与具体知识库节点授权的两层关系；完整测试基线提升为 46 项通过。

## 2026-07-15 — 监听器运行安全

- 显式固定群聊必须直接 `@` 机器人、`@所有人` 不触发处理的消息准入策略。
- 统一 Lark SDK 与项目日志管线，消除控制台重复记录，抑制带用户标识的 HTTP INFO 日志，并对 WebSocket 临时凭证做兜底脱敏。
- 新增跨平台单实例文件锁，防止旧监听器未停止时再次启动造成重复回复或并发下载。
- 补齐运行安全实施计划、README、架构、日志和测试目录说明；完整离线测试基线提升为 50 项通过。
