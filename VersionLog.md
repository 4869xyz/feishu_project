# Version Log

记录已经落地的业务能力、配置语义与工程规则变更。同一自然日的后续变更追加到现有日期块；纯格式调整不记录。

## 2026-07-14 — v0.1.0 基线

- 建立飞书长连接机器人，可接收聊天中直接上传的 `.xlsx`、`.xls`、`.xlsm` 附件。
- 通过 IM 消息资源接口获取文件，以大小限制、文件名清理、`.part` 原子落盘和重复消息幂等策略保存至 `data/inbox/`。
- 建立离线 pytest，覆盖配置、鉴权下载、附件解析和幂等行为。
- 建立 Cursor 协作规则、架构地图、目录职责说明和功能计划模板。

### 表格链接导出

- 支持 `*.feishu.cn/sheets/<token>` 直接导出 XLSX，并支持 `*.feishu.cn/wiki/<token>` 先解析 Wiki 节点再导出。
- Wiki 仅接受真实对象类型为 `sheet` 或 `bitable`；使用节点返回的 `obj_token` 创建导出任务，不把 Wiki Token 当作文档 Token。
- 新增导出任务创建、轮询和原子下载客户端能力；链接导出归档至 `data/archive/YYYY-MM/sender_open_id/`，Wiki 标题优先。
- 新增 `FEISHU_ARCHIVE_DIR` 配置、归档产物 Git 忽略规则、Wiki 权限不足的明确机器人提示，以及对应的 API/解析/监听器离线测试。
- 文档补充 Wiki 和云文档导出权限、开放平台 Scope 与具体知识库节点授权的两层关系；完整测试基线提升为 46 项通过。

## 2026-07-15 — 监听器运行安全

- 显式固定群聊必须直接 `@` 机器人、`@所有人` 不触发处理的消息准入策略。
- 统一 Lark SDK 与项目日志管线，消除控制台重复记录，抑制带用户标识的 HTTP INFO 日志，并对 WebSocket 临时凭证做兜底脱敏。
- 新增跨平台单实例文件锁，防止旧监听器未停止时再次启动造成重复回复或并发下载。
- 补齐运行安全实施计划、README、架构、日志和测试目录说明；完整离线测试基线提升为 50 项通过。

### 销售工作簿自动汇总

- 新增 SOP 驱动的 `.xlsx` 汇总引擎：严格校验 `Sheet1`/`Sheet2`，清空模板中签约和回款目标表的旧业务内容，再按上传顺序重建明细、个人/小组/部门统计、合并单元格和公式。
- 同一来源文件只接收一次；不同来源中的同名或相同内容记录不去重。签约、回款控制总额使用独立十进制计算核对，输出采用临时文件和原子替换。
- 模板的非目标工作表保持不变，并在 `openpyxl` 保存后恢复图片、绘图关系等非目标 OOXML 部件。
- 新增按“聊天 + 发送人”隔离的持久化汇总批次，以及 `汇总状态`、`清空汇总`、`汇总` 命令；生成成功并发回飞书后才清空批次。
- 新增 `FEISHU_AGGREGATION_DIR` 和必填的 `FEISHU_SALES_TEMPLATE_PATH` 配置，完整离线测试基线提升为 57 项通过。

## 2026-07-16 — 签约独立汇总

- 源工作簿只有一个工作表时直接作为签约表；多工作表时依次匹配精确 `签约情况`、精确 `签约数据`、包含“签约+情况”、包含“签约+数据”的名称，同级取工作簿中最靠前者。
- 上传校验结果会返回实际选中的签约工作表名称和有效明细数；没有候选表时列出现有工作表，避免误选签约汇总或排名表。
- 当前阶段暂停回款处理：不读取或校验源回款表，不要求模板存在回款目标表，也不在飞书回复中展示回款指标。
- 输出只清空并重建签约目标表；模板回款表和其他非目标工作表连同 OOXML 关系部件原样保留，回款错误不再阻断签约汇总。
- 离线测试覆盖选表优先级、实际选表回复、回款隔离和无回款模板，完整基线提升为 68 项通过。

### 下载缓存安全清理

- 新增机器人命令 `清空下载缓存`，清理收件箱、链接归档和历史汇总结果中的非活动文件，并回复删除数量、释放空间、活动文件保护数和失败数。
- 新增 `FEISHU_CACHE_ADMIN_OPEN_IDS` 管理员白名单；未配置时命令禁用，非管理员调用不会扫描或删除文件。
- 清理前汇总所有聊天和发送人的活动批次源路径；活动源文件、模板、批次状态和 `.gitkeep` 均受保护，单文件失败不阻断其余清理。
- 汇总与清理复用文件任务锁，避免在生成或发送汇总文件时并发删除；完整离线测试基线提升为 74 项通过。

### 固定云表汇总时刷新

- 新增 `添加云表 <链接>`、`云表列表`、`移除云表 <编号>` 命令；固定来源按“会话 + 发送人”隔离并按登记顺序长期保留。
- 汇总前顺序重新导出和校验全部固定 Sheets/Wiki 来源，固定来源排在临时附件之前；Wiki 节点每次刷新重新解析真实对象。
- 刷新文件先写 staging，通过销售表校验后才原子替换 latest；任一来源权限、导出或结构失败时整体中止，明确禁止使用旧缓存。
- 批次状态升级为 v2 并自动迁移 v1；成功汇总和 `清空汇总` 只清临时来源，固定登记及 latest 缓存保留。
- 离线测试覆盖状态迁移、登记生命周期、指定路径导出、刷新顺序和旧缓存失败保护，完整基线提升为 85 项通过。

### 签约非业务行与源字体颜色兼容

- 签约源表中九类个人、小组、部门月度/季度/年度合计行会在组别处理前跳过；其他 C 列人员为空或只有空格的行也静默跳过，不再校验其 I:T 内容。
- 每条有效签约明细保存 A:T 的 RGB、主题色、索引色、自动色和 tint；写入目标表时先应用模板完整样式，再仅替换字体颜色，不复制源字体名称、字号、粗体或其他格式。
- A 列组别由空值继承时同步继承最近一次非空组别单元格的字体颜色；个人、小组、部门汇总行和空行继续使用模板颜色。
- 修复真实模板保留非目标工作表时共享字符串部件及工作簿关系丢失的问题，确保生成结果可以正常重新打开。
- 离线测试覆盖逐单元格多色、多来源同内容异色、主题/索引/自动颜色、组别颜色继承、模板样式隔离和真实模板重开，完整基线提升为 88 项通过。

### 签约汇总区块空行

- 每个小组最后一名人员的“个人年度合计”与“小组月度合计”之间固定插入一行模板空行；最后一个“小组年度合计”与“部门月度合计”之间同样插入一行。
- 明细与个人合计、人员之间、不同小组之间的原有布局保持不变；空行不写值或公式，并统一取消隐藏、折叠和大纲状态。
- 公式引用、A 列小组合并范围和业务末行随新增行重新计算，完整离线测试仍为 88 项通过。

## 2026-07-20 — 固定云表批量管理

- 固定云表对外统一显示按当前顺序生成的连续数字编号；添加、列表、状态、删除和刷新错误回复不再暴露复杂 `cloud-...` ID，旧 ID 删除方式继续兼容。
- 新增 `云表排序 <完整编号顺序>`，在编号完整、唯一且不越界时持久化新顺序，后续刷新与汇总严格采用该顺序。
- 新增 `清空云表`，一次清除当前“会话 + 发送人”的全部固定登记及 latest 缓存，不影响临时汇总文件和其他所有者。
- 离线测试覆盖持久化重排、输入校验、数字/旧 ID 删除、批量清空、缓存删除失败和临时文件保留，完整基线提升为 92 项通过。

### 关键字段黄色/橙色明细自动隐藏

- 静态 RGB、主题色和索引字体色统一解析为显示 RGB，并按 `20°–70°` HSV 色相范围识别不同深浅的黄色与橙色。
- 隐藏判定收窄到人员、客户简称、产品/项目、落地地点、客户来源 C:G；任一字段命中即隐藏，A:B、H:T 的颜色不参与判定。
- 隐藏只改变行可见性，明细值、源字体颜色、公式范围和控制总额全部保留；汇总行及空行始终可见。
- 修复整行颜色占比造成的月份列黄色误隐藏，以及关键字段暖色可能被其他列颜色抵消的问题；离线测试覆盖 C:G 每列触发、非关键列隔离、RGB/主题/索引/tint、数据保留和汇总公式，完整基线提升为 95 项通过。
- 2026-07-21 修复空白 C:G 单元格残留黄色样式导致黑色业务文字行被隐藏的问题：只有内容非空的关键字段才参与暖色判断；同时纠正主题索引 `dk1、lt1、dk2、lt2` 顺序。当前缓存隔离验收由误判的 42 条隐藏修正为 41 条，完整基线提升为 103 项通过。

### I:T 金额会计专用一位小数

- 签约明细及个人、小组、部门月度/季度/年度金额统一覆盖为无货币符号、1 位小数的会计专用格式，空白明细月份同步预设。
- 保持源数值、公式、字体颜色、暖色隐藏、合并范围和控制总额不变；间隔空行与 A:H 文本区域继续使用模板格式。
- 输出重开时校验所有 I:T 数字和公式单元格的格式；离线测试覆盖整数、零、负数、小数、空白、汇总公式和隐藏行并存，完整基线提升为 96 项通过。

### I:T 金额列自适应宽度

- 每个月份列按全部明细金额绝对值上界的会计格式显示长度独立计算宽度，保留模板原宽度和 12 字符下限，只在需要时向外扩展。
- I:T 全部写入 `bestFit`，季度和年度合并区域继续使用累计列宽，不改变数值、公式、格式、隐藏行或其他列布局。
- 输出重开时校验实际宽度和 `bestFit`；离线测试覆盖大额、负数、月份独立扩展及较宽模板列保留，完整基线提升为 97 项通过。

### 固定云表链接批量登记

- `添加云表` 支持在同一条消息中提交多个 Sheets/Wiki 链接，链接可使用空格或换行分隔，并按首次出现顺序登记。
- 同一消息重复链接和已有固定来源自动跳过；每条新链接独立执行导出、结构校验和缓存提交，单项失败不会回滚或阻断其他有效链接。
- 批量命令统一回复成功、已存在、失败数量及新增数字编号，错误说明不回显链接 Token 或底层敏感信息；原单链接命令及临时链接流程保持兼容。
- 离线测试覆盖多链接顺序/去重、Sheets/Wiki 混合登记、已有来源跳过和部分失败继续，完整基线提升为 101 项通过。

### Windows 免安装便携交付

- 新增 Windows x64 PyInstaller `onedir` 构建流程，发布包携带当前 `.env`、模板和空运行目录，目标电脑无需安装 Python。
- 新增中文一键启动、停止和日志查看入口；打包运行时以可执行文件目录解析配置和相对数据路径，源码运行方式保持不变。
- `release/`、PyInstaller `build/` 和 `dist/` 产物统一由 Git 忽略；最终 98 项测试通过，便携 EXE 配置加载与真实飞书长连接均完成验证。
- 2026-07-21 再次构建便携包，将批量添加云表、空白黄色样式防误隐藏、主题色映射、金额格式、列宽和云表管理更新同步到冻结 EXE；随包使用说明补充全部常用命令和隐藏规则。完整 103 项测试通过，ZIP 共 571 个文件、16,806,921 字节，SHA-256 为 `05187BF4AE397770115234CD34B7EEB98ED7445704991484FE0B959265A0674D`。
## 2026-08-05 — 独立周例会纪要机器人首版

- 新增 `meeting_minutes_bot` 独立长连接入口，使用新的飞书应用、`.env.meeting-minutes`、私聊策略、日志与单实例锁；原销售机器人入口和业务代码保持不变。
- 新增基于人员 YAML 和发送者 `open_id` 的身份识别，支持文字追加、替换、查看、撤回，以及管理员提交状态和生成本周纪要命令。
- 新增 SQLAlchemy + aiosqlite 三表存储，按 `message_id` 保证变更事件幂等；原文、人员快照、替换/撤回历史和 DOCX 版本记录均可追溯。
- 新增 `docxtpl` 模板校验和版本化 Word 输出；正式模板缺失或缺少启用人员占位符时拒绝启动，仓库测试模板仅用于自动化验证。
- 新增 15 项离线测试，覆盖配置隔离、身份权限、ISO 周边界、并发、幂等、模板和双锁；完整测试基线提升为 118 项通过。

## 2026-08-06 — 纪要附件识别、自动清理与文档同步

- 周例会纪要机器人新增图片本地 OCR，以及文字型 PDF、DOCX、Markdown 提取；附件按消息幂等追加，不支持的格式、扫描版 PDF、超限文件或超长识别结果会明确拒绝。
- 新增统一数据保留策略：附件缓存、数据库业务记录和生成的 Word 默认保留 14 天，启动时及之后每 24 小时清理；日志按天轮转并按同一保留期保留。
- 新增附件大小、PDF 页数、保留天数和缓存容量配置校验，并补充附件处理与清理的离线测试。
- 同步主 README、架构地图、数据目录和纪要使用说明，补回不含凭据与真实 `open_id` 的 `.env.meeting-minutes.example` 和人员 YAML 示例。

## 2026-08-08 — 周日未提交提醒

- 周例会纪要机器人新增进程内周日提醒：`Asia/Shanghai` 周日 17:00 / 20:00 私聊所有 `enabled` 且本周无有效提交的人员；20:00 仅再提醒仍未提交者，不向管理员发汇总。
- 新增 `meeting_reminder_runs` 表保证「周期 + 槽位」幂等，支持宕机后补发与超时 `PROCESSING` 重试；可用 `MEETING_BOT_REMINDER_ENABLED=false` 关闭。
- 保留期清理同步删除过期提醒轮次；离线测试覆盖槽位判定、名单差异、幂等、失败隔离和配置开关。

### 人员配置热重载

- 新增管理员命令 `重载人员配置`：修改 `people.yaml` 后无需重启进程，机器人重新读取 YAML 并校验启用人员的模板占位符，通过后即时热替换人员名单。
- 校验或读取失败时保留原名单并回复具体错误；提交权限、提醒名单和 DOCX 渲染统一经共享 `PeopleStore` 读取，重载后立即生效。
- 人员 YAML 仍是唯一配置源，不引入人员入库或飞书增删改命令；离职置 `enabled: false`，新人顶替旧岗位改绑该槽位 `open_id` 即可，无需改 Word 模板。
- 新增 5 项重载离线测试（权限、热生效、坏 YAML 回退、缺占位符回退、无路径报错）；纪要测试基线提升为 55 项通过、1 项跳过。

### 纪要机器人 Windows 便携发布包

- 新增纪要机器人独立的 Windows x64 免安装交付：`packaging/windows/meeting/` 下的 spec、构建脚本、中文启停与日志入口，与销售机器人的便携包完全隔离。
- `meeting_minutes_bot/settings.py` 支持冻结运行时，以 `MeetingMinutesBot.exe` 所在目录为项目根解析配置、模板、数据库与日志；新增顶层入口 `run_meeting_minutes_bot.py`，`python -m meeting_minutes_bot` 用法不变。
- 打包随包收集 RapidOCR 的 ONNX 模型、onnxruntime 与 PyMuPDF 原生库和 `tzdata`，确保目标电脑离线可用、不在运行时下载模型。
- 发布包原样复制当前 `.env.meeting-minutes`、人员 YAML 和正式模板，运行数据目录一律为空，不携带历史提交、附件缓存或已生成的 DOCX；构建脚本额外输出文件数、字节数和 SHA-256。
- 构建脚本在 `.venv` 缺少 pip 时自动改用 `uv` 安装固定版本 PyInstaller，并校验人员配置与模板路径必须为相对路径。
- 补充随包傻瓜式使用说明与《便携发布包构建与交付》文档，明确同一飞书应用同一时刻只能运行一个实例，以及发布包含密钥不得公开分发。
- 新增 2 项冻结路径测试；完整测试基线提升为 160 项通过、1 项跳过。发布包 1987 个文件、154,823,786 字节。

### DOCX 表格与图片原样嵌入周报

- 用户私聊发送的 `.docx` 会持久保存到 `data/meeting_minutes/submission_docs/`；生成周报时按人员段落注入正文、表格与内嵌图片，不再只保留纯文本摘要。
- 仅含表格/图片、几乎无正文的 Word 也可提交；「查看我的纪要」仍显示文字摘要并提示含表格/图片。
- 独立飞书图片消息继续 OCR；页眉页脚、浮动对象等复杂版式不做完整还原。合并失败时回退该条文字摘要，不阻断整份周报。
- 保留期清理同步删除过期源 Word；新增 4 项嵌入测试，纪要测试基线提升为 61 项通过、1 项跳过。
- 2026-08-11 同步重建纪要 Windows 便携包（含 DOCX 原样嵌入）：完整测试 164 通过 1 跳过；ZIP 3073 个文件、165,098,749 字节，SHA-256 `4B09EF3140F81D0EC2C42454ACACE917ADA529286830E36B5699EBE252B107DE`。

## 2026-08-12 — 管理员私聊上传配置热生效

- 管理员私聊上传 `.yaml`/`.yml` 可校验后热更新人员名单；上传与正式模板同名的 `.docx` 可校验后热更新 Word 模板，均无需重启。
- 更新前自动备份到 `data/meeting_minutes/config_backups/`（保留最近 20 份）；校验失败不覆盖线上文件。管理员用异名 `.docx` 仍按本周提交处理。
- 新增管理员指令 `校验配置` 做只读体检；保留磁盘改文件后的 `重载人员配置`。
- 管理员说明补充服务器单实例、进程守护与备份建议；纪要离线测试基线提升为 71 项通过、1 项跳过。

### Linux（Ubuntu）便携发布包

- 新增 `packaging/linux/meeting/`：在 Ubuntu 24.04 等 Linux x64 上用 PyInstaller 构建纪要机器人 onedir 便携包，产出 `release/周例会纪要机器人-Linux-x64.tar.gz`。
- 随包提供 `启动机器人.sh` / `停止机器人.sh` / `查看运行日志.sh` 与 `meeting-minutes-bot.service.example`（systemd 开机自启与崩溃拉起）。
- **必须在 Linux 上构建**，不能从 Windows 交叉编译；构建与交付说明见 `docs/meeting_minutes/Linux便携发布包构建与交付.md`。
- 与 Windows 便携包同样复制当前 `.env.meeting-minutes`、人员 YAML 与模板，数据目录为空；同一飞书应用仍只能单实例运行。

### Ubuntu 源码 systemd 部署与升级 SOP

- 新增 `meeting-minutes-bot.source.service.example`（`python -m meeting_minutes_bot` + venv），与便携包 unit 均配置 `Restart=always` 以及 **60 秒内最多自动重启 5 次**，超限停止并等待管理员 `reset-failed` 后处理。
- 新增 `docs/meeting_minutes/Ubuntu源码systemd部署与升级.md`：首次部署、变更分类（热更新 vs 重启）、标准 `git pull` 升级/回滚与限流恢复步骤。
- 管理员说明改为推荐固定服务器走源码 + systemd；便携包仍作免 Python 交付备选。
