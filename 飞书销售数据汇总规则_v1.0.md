# 飞书多维表格销售数据汇总规则 v1.0

## 1. 文档目的

本文档用于指导 Python 程序将飞书多维表格记录转换为销售数据汇总 Excel。

当前规则参考以下文件：

- `2026年销售数据汇总SOP.md`
- `feishu_connection_report.json`
- `（实验）2026.07.03 2026年销售数据统计(1).xlsx`

最终输出工作簿应保持参考 Excel 的整体展示形式，并生成以下两个工作表：

1. `展示用-2026年签约数据汇总（0702统计）`
2. `展示用-2026年回款数据汇总(0702统计)`

本文档只定义数据处理、汇总、校验和 Excel 展示规则，不包含飞书 API 连接代码。

---

## 2. 当前输入数据说明

### 2.1 签约数据来源

飞书表名：

```text
签约标准明细表
```

当前 `table_id`：

```text
tbl16fMJCW3pnZ0A
```

字段共 19 个：

```text
组别
人员
客户简称
销售产品名称|项目名称
落地地点
客户来源（展会需注明）
备注
1月
2月
3月
4月
5月
6月
7月
8月
9月
10月
11月
12月
```

### 2.2 回款数据来源

连接报告中的表名目前是：

```text
签约个人汇总表
```

当前 `table_id`：

```text
tblfOvj2TE61yMXE
```

但该表字段实际上属于**回款明细数据**，不是“签约个人汇总数据”。

建议后续在飞书和代码配置中将其更名为：

```text
回款明细表
```

或至少在代码中使用变量名：

```text
PAYMENT_DETAIL_TABLE_ID
```

当前字段共 24 个：

```text
姓名
收款主体
付款主体
产品
合同总价
居间费
已回款-提成口径（26年以前）
已回款-提成口径（26年）
已回款-提成口径总额
待回款额
回款比例
预计剩余回款时间及金额
1月
2月
3月
4月
5月
6月
7月
8月
9月
10月
11月
12月
```

### 2.3 当前 JSON 文件的使用限制

`feishu_connection_report.json` 是连接检查报告，每张表只包含最多 3 条样例记录。

因此：

- 它可以用于开发字段解析和小规模测试。
- 它不能作为正式年度汇总的完整数据源。
- 正式程序必须通过飞书 API 分页获取全部记录，或者使用包含全部记录的 JSON 文件。
- 程序不得把 `sample_records` 当成完整数据集。

---

## 3. 目标 Excel 使用原则

参考 Excel 主要作为：

1. 工作表名称参考；
2. 表头结构参考；
3. 合并单元格参考；
4. 字体、颜色、边框、对齐方式参考；
5. 个人、小组和部门汇总区域的展示参考。

参考 Excel 中已有公式存在以下问题：

- 部门汇总区域存在 `#REF!`；
- 回款表部门汇总存在大量 `#REF!`；
- 个别季度公式没有完整包含三个月；
- 个别年度公式没有完整包含十二个月；
- 部分旧数据区域存在人员和明细错位的风险。

因此：

> Python 程序只复用参考 Excel 的布局和样式，不得直接复制其旧公式作为新汇总逻辑。

推荐采用“模板样式 + Python 重新计算 + 动态重建数据区域”的方式生成结果。

---

# 第一部分：通用数据处理规则

## 4. 飞书字段解析规则

飞书 API 返回的字段值可能有多种形式。

### 4.1 文本列表

示例：

```json
[
  {
    "text": "傲翔组",
    "type": "text"
  }
]
```

解析结果：

```text
傲翔组
```

规则：

1. 如果值是列表，依次读取每个元素的 `text`；
2. 多个文本片段拼接为一个字符串；
3. 去除首尾空格；
4. 空列表、空字符串和 `null` 统一转换为空值；
5. 不保留飞书返回的外层列表结构。

### 4.2 数字标量

示例：

```json
50000
```

直接转换为 `Decimal`。

### 4.3 文本数字

示例：

```json
[
  {
    "text": "50000",
    "type": "text"
  }
]
```

金额字段应转换为：

```text
Decimal("50000")
```

### 4.4 缺失月份

如果某条记录没有返回某个月份字段，表示该月份没有金额。

统一处理为：

```text
0
```

不能因为字段缺失而跳过整条记录。

---

## 5. 文本标准化规则

所有文本字段统一执行：

1. 去除首尾空格；
2. 将空字符串转换为 `None`；
3. 保留文本内部换行；
4. 保留中文、英文、数字和标点；
5. 不主动修改客户名称、项目名称和备注内容；
6. 不自动把人员别名映射成其他人员；
7. 不自动合并姓名相同但组别不同的人员。

用于分组匹配的字段可额外执行：

- 将连续多个空格压缩为一个；
- 去除姓名和组别首尾不可见字符。

---

## 6. 金额标准化规则

所有金额必须使用 `Decimal`，不能使用 `float` 参与汇总。

需要支持以下输入：

```text
50000
"50000"
"50,000"
"￥50000"
"50000元"
" 50000 "
```

统一转换为：

```text
50000
```

以下值按空值或零处理：

```text
空
null
""
"/"
"-"
"无"
"暂无"
```

### 6.1 无法转换的金额

例如：

```text
2026/02/01
abc
待确认
```

不能静默转换为数字。

必须：

1. 标记该字段为转换失败；
2. 将错误写入处理报告；
3. 根据字段重要性决定：
   - 明细仍然输出，但异常金额留空；
   - 或将该记录放入失败记录，不进入正式金额汇总。

---

## 7. 月份字段规则

统一月份字段列表：

```python
MONTH_FIELDS = [
    "1月", "2月", "3月", "4月", "5月", "6月",
    "7月", "8月", "9月", "10月", "11月", "12月"
]
```

季度映射：

| 季度 | 月份 |
|---|---|
| Q1 | 1月、2月、3月 |
| Q2 | 4月、5月、6月 |
| Q3 | 7月、8月、9月 |
| Q4 | 10月、11月、12月 |

年度金额：

```text
年度合计 = 1月 + 2月 + ... + 12月
```

---

## 8. 记录唯一标识

每条飞书记录必须保留：

```text
record_id
```

`record_id` 用于：

- 异常追踪；
- 日志定位；
- 避免重复处理；
- 生成审计报告。

Excel 展示表中可以不显示 `record_id`，但程序内部数据模型和处理报告中必须保留。

---

## 9. 排序规则

### 9.1 组别排序

优先级：

1. 如果配置文件定义了 `GROUP_ORDER`，按配置顺序；
2. 参考模板中已有组别按模板顺序；
3. 新增组别按第一次出现顺序追加；
4. 不建议默认按汉字拼音排序，以免改变业务人员习惯。

建议配置：

```python
GROUP_ORDER = ["傲翔组", "冉哥组", "云子组"]
```

当前数据出现但未配置的组别，例如 `凌云组`，追加在已配置组别之后。

### 9.2 人员排序

同一组别内：

1. 已存在于模板配置中的人员按配置顺序；
2. 新人员按飞书记录第一次出现顺序；
3. 不同组别中的同名人员分别汇总。

### 9.3 明细排序

同一人员的明细默认按照：

```text
created_time 升序
```

如果 `created_time` 不存在，则按照 API 返回顺序。

不得按金额大小重新排序。

---

# 第二部分：签约数据汇总规则

## 10. 目标签约工作表结构

工作表名称：

```text
展示用-2026年签约数据汇总（0702统计）
```

列范围：

```text
A:T
```

### 10.1 表头结构

| 列 | 内容 |
|---|---|
| A | 组别 |
| B | 序号 |
| C | 人员 |
| D | 客户简称 |
| E | 销售产品名称/项目名称 |
| F | 落地地点 |
| G | 客户来源（展会需注明） |
| H | 备注 |
| I | 1月 |
| J | 2月 |
| K | 3月 |
| L | 4月 |
| M | 5月 |
| N | 6月 |
| O | 7月 |
| P | 8月 |
| Q | 9月 |
| R | 10月 |
| S | 11月 |
| T | 12月 |

表头合并规则：

- A2:A3 合并；
- B2:B3 合并；
- C2:C3 合并；
- D2:D3 合并；
- E2:E3 合并；
- F2:F3 合并；
- G2:G3 合并；
- H2:H3 合并；
- I2:T2 合并，显示：

```text
签约后且收到第一笔款后才算已签约
```

- I3:T3 分别显示 1月至12月；
- 数据从第 4 行开始。

---

## 11. 签约字段映射

| 飞书字段 | Excel 列 | 处理规则 |
|---|---:|---|
| 组别 | A | 用于分组，组别区域内合并显示 |
| 无对应字段 | B | Python 自动生成序号 |
| 人员 | C | 直接写入 |
| 客户简称 | D | 直接写入 |
| 销售产品名称\|项目名称 | E | 写入“销售产品名称/项目名称” |
| 落地地点 | F | 直接写入 |
| 客户来源（展会需注明） | G | 直接写入 |
| 备注 | H | 直接写入 |
| 1月 | I | 金额 |
| 2月 | J | 金额 |
| 3月 | K | 金额 |
| 4月 | L | 金额 |
| 5月 | M | 金额 |
| 6月 | N | 金额 |
| 7月 | O | 金额 |
| 8月 | P | 金额 |
| 9月 | Q | 金额 |
| 10月 | R | 金额 |
| 11月 | S | 金额 |
| 12月 | T | 金额 |

### 11.1 序号生成

飞书签约表没有序号字段，因此 B 列由程序生成。

规则：

- 每个人员的明细从 1 开始连续编号；
- 只对明细行编号；
- 个人汇总行、空行、小组汇总行不编号；
- 不使用飞书返回顺序作为永久业务编号。

---

## 12. 签约记录有效性

### 12.1 必填字段

至少必须有：

```text
组别
人员
```

缺少组别或人员的记录：

- 不进入正式汇总；
- 写入失败记录；
- 记录 `record_id` 和失败原因。

### 12.2 明细保留规则

即使客户简称、项目名称、地点、来源或备注为空，只要组别和人员有效，该记录仍可进入明细。

### 12.3 全年金额均为零

如果一条记录 12 个月金额全部为 0：

- 默认仍保留明细，因为它可能是正在跟进的销售机会；
- 该记录不增加金额汇总；
- 可通过配置项决定是否过滤：

```python
KEEP_ZERO_AMOUNT_SIGNING_RECORDS = True
```

---

## 13. 签约分组结构

每个组别按以下顺序生成：

```text
组别
  人员1明细
  人员1个人月度合计
  人员1个人季度合计
  人员1个人年度合计
  空一行
  人员2明细
  人员2个人月度合计
  人员2个人季度合计
  人员2个人年度合计
  空一行
  小组月度合计
  小组季度合计
  小组年度合计
空一整行
下一个组别
```

最后生成：

```text
部门月度合计
部门季度合计
部门年度合计
```

### 13.1 只有一个人员的组别

即使组内只有一个人员，也必须生成：

- 个人汇总；
- 小组汇总；
- 部门汇总。

不得因为只有一个人而省略小组汇总。

---

## 14. 个人汇总规则

### 14.1 个人月度合计

对该人员的所有明细按月求和。

示例：

```text
个人1月合计 = 该人员全部明细的1月金额之和
个人2月合计 = 该人员全部明细的2月金额之和
...
个人12月合计 = 该人员全部明细的12月金额之和
```

Excel 展示：

- B:H 合并；
- 显示 `个人月度合计：`；
- I:T 分别写入 12 个月金额。

### 14.2 个人季度合计

```text
Q1 = 1月 + 2月 + 3月
Q2 = 4月 + 5月 + 6月
Q3 = 7月 + 8月 + 9月
Q4 = 10月 + 11月 + 12月
```

Excel 合并：

| 季度 | 合并范围 |
|---|---|
| Q1 | I:K |
| Q2 | L:N |
| Q3 | O:Q |
| Q4 | R:T |

B:H 合并并显示：

```text
个人季度合计：
```

### 14.3 个人年度合计

```text
个人年度合计 = 12个月个人月度合计之和
```

Excel：

- B:H 合并；
- 显示 `个人年度合计：`；
- I:T 合并；
- 在合并区域中显示年度金额。

---

## 15. 小组汇总规则

每个组别固定生成 3 行：

```text
小组月度合计：
小组季度合计：
小组年度合计：
```

计算规则：

```text
小组月度合计 = 组内所有人员对应月份金额之和
小组季度合计 = 组内所有人员对应季度金额之和
小组年度合计 = 组内所有人员年度金额之和
```

禁止通过加总已经合并的季度显示单元格来推断月度值。

程序内部应以原始月度数组作为唯一计算基础。

---

## 16. 部门汇总规则

所有组别结束后生成：

```text
部门月度合计：
部门季度合计：
部门年度合计：
```

计算规则：

```text
部门月度合计 = 所有有效签约明细对应月份之和
部门季度合计 = 部门月度合计按季度相加
部门年度合计 = 12个月部门月度合计之和
```

为了避免重复累计，建议直接根据全部有效明细计算部门合计，而不是再次加总展示区域中的 Excel 单元格。

---

## 17. 签约合并单元格规则

### 17.1 组别列

A 列按整个组别区域合并。

合并范围包括：

- 该组全部人员明细；
- 所有个人汇总行；
- 同组人员之间的空行；
- 小组汇总行。

组别之间的分隔空行不纳入合并范围。

### 17.2 个人汇总

- 月度行：B:H 合并；
- 季度行：B:H、I:K、L:N、O:Q、R:T 分别合并；
- 年度行：B:H、I:T 分别合并。

### 17.3 小组汇总

与个人汇总相同：

- 月度行：B:H 合并；
- 季度行：B:H、I:K、L:N、O:Q、R:T 分别合并；
- 年度行：B:H、I:T 分别合并。

### 17.4 部门汇总

建议：

- 月度行：A:H 合并；
- 季度行：A:H、I:K、L:N、O:Q、R:T 分别合并；
- 年度行：A:H、I:T 分别合并。

---

## 18. 签约样例验算

根据当前 JSON 样例：

### 18.1 凌云组—李一

明细：

- 1月：5000
- 3月：60000

应得到：

```text
1月合计 = 5000
2月合计 = 0
3月合计 = 60000
Q1 = 65000
年度合计 = 65000
```

### 18.2 傲翔组—李四

两条样例记录合并后：

第一条：

- 1月：50000
- 7月：50000

第二条：

- 1月：10000
- 5月：6000
- 12月：6000

个人月度合计：

```text
1月 = 60000
5月 = 6000
7月 = 50000
12月 = 6000
其他月份 = 0
```

个人季度合计：

```text
Q1 = 60000
Q2 = 6000
Q3 = 50000
Q4 = 6000
```

个人年度合计：

```text
122000
```

该验算结果可作为签约汇总单元测试。

---

# 第三部分：回款数据汇总规则

## 19. 目标回款工作表结构

工作表名称：

```text
展示用-2026年回款数据汇总(0702统计)
```

列范围：

```text
A:AC
```

### 19.1 目标列定义

| 列 | 字段 |
|---|---|
| A | 姓名 |
| B | 收款主体 |
| C | 付款主体 |
| D | 产品 |
| E | 合同日期 |
| F | 合同总价 |
| G | 居间费 |
| H | 已回款-提成口径（26年以前） |
| I | 已回款-提成口径（26年） |
| J | 已回款-提成口径总额 |
| K | 待回款额 |
| L | 已开发票金额 |
| M | 已开票未回款 |
| N | 回款比例 |
| O | 税/运费 |
| P | 预计剩余回款时间及金额 |
| Q | 备注 |
| R | 1月 |
| S | 2月 |
| T | 3月 |
| U | 4月 |
| V | 5月 |
| W | 6月 |
| X | 7月 |
| Y | 8月 |
| Z | 9月 |
| AA | 10月 |
| AB | 11月 |
| AC | 12月 |

### 19.2 表头合并

- A1:Q1 合并，显示 `2026年回款数据汇总`；
- R2:AC2 合并，显示 `已回款-业绩口径(26年)`；
- A2:A3 到 Q2:Q3 分别纵向合并；
- R3:AC3 分别显示 1月至12月；
- 明细从第 4 行开始。

---

## 20. 回款字段映射

| 飞书字段 | Excel 列 | 规则 |
|---|---:|---|
| 姓名 | A | 直接写入 |
| 收款主体 | B | 直接写入 |
| 付款主体 | C | 直接写入 |
| 产品 | D | 直接写入 |
| 当前无字段 | E | 合同日期留空 |
| 合同总价 | F | 转换为 Decimal |
| 居间费 | G | 转换为 Decimal |
| 已回款-提成口径（26年以前） | H | 转换为 Decimal，`/`、`无`视为0 |
| 不直接使用源值 | I | 根据 1月至12月重新计算 |
| 不直接使用源值 | J | 根据 H + I 重新计算 |
| 不直接使用源值 | K | 根据 F - J 重新计算 |
| 当前无字段 | L | 已开发票金额留空 |
| 当前无字段 | M | 已开票未回款留空 |
| 不直接使用源值 | N | 根据 J / F 重新计算 |
| 当前无字段 | O | 税/运费留空 |
| 预计剩余回款时间及金额 | P | 保留源内容 |
| 当前无字段 | Q | 备注留空 |
| 1月 | R | 转换为 Decimal |
| 2月 | S | 转换为 Decimal |
| 3月 | T | 转换为 Decimal |
| 4月 | U | 转换为 Decimal |
| 5月 | V | 转换为 Decimal |
| 6月 | W | 转换为 Decimal |
| 7月 | X | 转换为 Decimal |
| 8月 | Y | 转换为 Decimal |
| 9月 | Z | 转换为 Decimal |
| 10月 | AA | 转换为 Decimal |
| 11月 | AB | 转换为 Decimal |
| 12月 | AC | 转换为 Decimal |

### 20.1 缺失字段处理

当前飞书表缺少以下目标字段：

```text
合同日期
已开发票金额
已开票未回款
税/运费
备注
```

第一版处理：

- Excel 中保留对应列；
- 明细值留空；
- 不根据其他字段猜测；
- 不把“合同总价”中的日期文本自动挪到“合同日期”。

后续建议在飞书回款明细表中补充这些字段。

---

## 21. 回款派生字段计算

### 21.1 2026年已回款

```text
I = 1月 + 2月 + ... + 12月
```

即：

```text
I = SUM(R:AC)
```

源字段 `已回款-提成口径（26年）` 只用于校验，不作为最终结果。

### 21.2 已回款总额

```text
J = H + I
```

### 21.3 待回款额

```text
K = F - J
```

允许出现负值，负值代表回款总额高于合同总价。

不能默认强制截断为 0，除非业务方另行确认。

### 21.4 回款比例

```text
N = J / F
```

规则：

- 当 F 为空或等于 0，N 留空；
- 不生成 `#DIV/0!`；
- Excel 以百分比格式显示；
- 内部值使用 0 至 1 的小数，例如 50% 存储为 `0.5`。

---

## 22. 回款源字段一致性校验

飞书中已经存在：

```text
已回款-提成口径（26年）
已回款-提成口径总额
待回款额
回款比例
```

这些字段可能由人工填写或历史公式生成。

Python 程序应：

1. 根据月份和合同总价重新计算；
2. 将重新计算结果写入目标 Excel；
3. 将源值与计算值进行对比；
4. 差异超过 0.01 时写入处理报告；
5. 不用不一致的源值覆盖程序计算结果。

校验项：

```text
源26年回款 vs 月份合计
源回款总额 vs H + 月份合计
源待回款额 vs 合同总价 - 回款总额
源回款比例 vs 回款总额 / 合同总价
```

---

## 23. 回款明细有效性

### 23.1 必填字段

至少必须有：

```text
姓名
```

没有姓名的记录不进入正式工作表。

### 23.2 合同总价异常

当前样例中出现：

```text
合同总价 = 2026/02/01
```

该值看起来像日期，不是金额。

处理规则：

- 标记为 `合同总价格式异常`；
- F 列留空；
- K 和 N 留空；
- 该条记录仍可展示其他字段和月份回款；
- 在处理报告中记录 `record_id`。

### 23.3 预计剩余回款时间及金额

该字段名称同时表达“时间”和“金额”。

当前飞书字段类型为数字时：

- 按原数值写入；
- 不自动推断日期。

后续建议将该字段改为文本，或拆分为：

```text
预计剩余回款时间
预计剩余回款金额
```

---

## 24. 回款人员结构

按姓名分组。

每个人的结构：

```text
人员明细1
人员明细2
...
个人小计
空一行
下一人员
```

个人小计行：

- D 列显示 `个人小计：`；
- 对可汇总的金额列求和；
- 不对文本列求和；
- 回款比例不直接求和。

### 24.1 个人小计字段

个人小计建议计算：

```text
合同总价
居间费
26年以前已回款
26年已回款
已回款总额
待回款额
已开发票金额
已开票未回款
税/运费
1月至12月回款
```

个人小计回款比例：

```text
个人已回款总额 / 个人合同总价
```

当个人合同总价为 0 时留空。

---

## 25. 回款部门总计

全部人员结束后生成：

```text
部门总计：
```

部门总计规则：

- D 列显示 `部门总计：`；
- 金额列为全部有效明细之和；
- 部门回款比例为：

```text
部门已回款总额 / 部门合同总价
```

不能把各人员的回款比例直接相加或简单平均。

---

## 26. 回款样例数据质量说明

当前 JSON 样例存在明显不一致，程序必须按规则重新计算。

### 26.1 样例：林雪

月份：

```text
1月 = 5000
2月 = 600
4月 = 1000
```

因此：

```text
26年回款 = 6600
```

不能直接采用源字段中的：

```text
已回款-提成口径（26年） = 50000
```

如果：

```text
合同总价 = 500000
26年以前回款 = 0
```

则：

```text
已回款总额 = 6600
待回款额 = 493400
回款比例 = 1.32%
```

### 26.2 样例：吴傲翔

出现：

```text
合同总价 = 2026/02/01
```

该值必须标记为金额格式异常。

月份样例：

```text
2月 = 1
7月 = 18700
```

月份合计应为：

```text
18701
```

如果源字段总额为 18700，应记录差异。

以上样例可作为回款规则的单元测试。

---

# 第四部分：Excel 展示格式规则

## 27. 颜色和字体

参考模板风格：

### 27.1 表头

- 浅绿色填充；
- 黑色粗体；
- 水平和垂直居中；
- 自动换行；
- 细边框。

### 27.2 明细

- 人员、客户和金额可沿用模板中的红色强调；
- 备注和长文本自动换行；
- 金额右对齐；
- 文本垂直居中。

### 27.3 汇总行

- 标签加粗；
- 小组汇总使用浅绿色填充；
- 部门汇总使用较深的浅绿色填充；
- 汇总金额使用黑色或模板指定的强调色。

---

## 28. 数字格式

金额统一格式：

```text
#,##0.00
```

零值展示建议：

```text
-
```

推荐自定义格式：

```text
#,##0.00;-#,##0.00;-
```

回款比例：

```text
0.00%
```

日期：

```text
yyyy-mm-dd
```

---

## 29. 行高和列宽

总体原则：

- 保留模板的列宽比例；
- 备注、客户来源、项目名称等长文本列开启自动换行；
- 明细行根据文本内容自适应行高；
- 汇总行保持统一高度；
- 不因一条超长备注无限扩大整张表列宽。

---

## 30. 空行规则

### 30.1 签约表

- 同一组别内人员之间空 1 行；
- 小组汇总后空 1 行；
- 部门汇总前不额外插入多余空白区域。

### 30.2 回款表

- 每个人员个人小计后空 1 行；
- 最后一名人员与部门总计之间空 1 行。

---

## 31. 隐藏列和隐藏行

参考回款表中部分字段列被隐藏。

第一版建议：

- 保留所有业务列；
- 是否隐藏合同日期、发票、税费、备注等列由配置控制；
- 不默认隐藏明细行；
- 不复制参考模板中历史遗留的隐藏行。

建议配置：

```python
HIDE_OPTIONAL_PAYMENT_COLUMNS = False
```

---

# 第五部分：计算和写入策略

## 32. 计算值优先于旧公式

推荐默认模式：

```text
Python 计算全部汇总值，并把数值写入 Excel。
```

优点：

- 不依赖 Excel 打开后重新计算；
- 避免公式引用错位；
- 避免插入行后产生 `#REF!`；
- 便于自动化验算。

如果确实需要公式模式：

1. 必须在所有明细和合并单元格完成后再写公式；
2. 公式引用必须根据动态行号生成；
3. 文件打开时强制重新计算；
4. 保存前扫描公式中是否存在 `#REF!`。

第一版建议采用数值模式。

---

## 33. 模板重建策略

推荐流程：

1. 复制参考 Excel 为新文件；
2. 保留两个工作表的表头、列宽和基础样式；
3. 清除旧的动态明细、旧汇总和旧公式；
4. 清除旧动态区域的合并单元格；
5. 根据当前飞书完整数据重建签约区域；
6. 根据当前飞书完整数据重建回款区域；
7. 重新创建合并单元格；
8. 写入 Python 计算后的汇总数值；
9. 应用模板样式；
10. 扫描错误值；
11. 保存为新文件。

不建议在旧明细中间反复插行并修补旧公式。

---

## 34. 输出文件命名

建议：

```text
2026年销售数据统计_YYYYMMDD_HHMMSS.xlsx
```

示例：

```text
2026年销售数据统计_20260711_173000.xlsx
```

不得覆盖原始模板。

---

## 35. 处理报告

每次生成 Excel 时，同时输出：

```text
processing_report.json
```

建议字段：

```json
{
  "run_time": "",
  "signing": {
    "input_count": 0,
    "valid_count": 0,
    "failed_count": 0,
    "group_count": 0,
    "person_count": 0,
    "annual_total": 0
  },
  "payment": {
    "input_count": 0,
    "valid_count": 0,
    "failed_count": 0,
    "person_count": 0,
    "contract_total": 0,
    "payment_total": 0
  },
  "warnings": [],
  "failed_records": []
}
```

失败记录至少包含：

```text
table_name
record_id
field_name
raw_value
error_reason
```

---

# 第六部分：结果校验规则

## 36. 签约校验

必须满足：

```text
每个人年度合计 = 该人员12个月合计
每个小组年度合计 = 该组所有人员年度合计
部门年度合计 = 所有小组年度合计
部门年度合计 = 全部有效明细12个月金额之和
```

按月也必须满足：

```text
部门某月合计 = 所有组别该月合计
```

---

## 37. 回款校验

每条明细：

```text
26年回款 = 1月至12月之和
回款总额 = 26年以前回款 + 26年回款
待回款额 = 合同总价 - 回款总额
回款比例 = 回款总额 / 合同总价
```

个人：

```text
个人小计 = 该人员全部明细之和
```

部门：

```text
部门总计 = 全部有效明细之和
```

---

## 38. Excel 错误扫描

生成后必须检查整个工作簿中是否存在：

```text
#REF!
#DIV/0!
#VALUE!
#NAME?
#N/A
```

只要存在上述错误，文件不能作为正式结果交付。

---

## 39. 数据数量校验

签约：

```text
有效明细行数 = 进入签约工作表的飞书有效记录数
```

回款：

```text
有效明细行数 = 进入回款工作表的飞书有效记录数
```

个人汇总、小组汇总、部门汇总和空行不计入明细数量。

---

# 第七部分：程序模块建议

## 40. 推荐模块

```text
services/
├── feishu_record_parser.py
├── signing_transformer.py
├── payment_transformer.py
├── signing_aggregator.py
├── payment_aggregator.py
├── excel_report_builder.py
├── validation_service.py
└── processing_report_service.py
```

职责：

| 模块 | 职责 |
|---|---|
| feishu_record_parser.py | 解析飞书列表文本、数字和空值 |
| signing_transformer.py | 签约字段清洗和标准化 |
| payment_transformer.py | 回款字段清洗和派生字段计算 |
| signing_aggregator.py | 人员、小组、部门签约汇总 |
| payment_aggregator.py | 个人和部门回款汇总 |
| excel_report_builder.py | 构建两个目标工作表 |
| validation_service.py | 执行金额、数量和公式错误校验 |
| processing_report_service.py | 输出 JSON 处理报告 |

---

## 41. 推荐内部数据结构

### 41.1 签约明细

```python
SigningRecord(
    record_id,
    group_name,
    person_name,
    customer_short_name,
    project_name,
    location,
    customer_source,
    remark,
    monthly_amounts
)
```

其中：

```python
monthly_amounts = {
    "1月": Decimal("0"),
    ...
    "12月": Decimal("0")
}
```

### 41.2 回款明细

```python
PaymentRecord(
    record_id,
    person_name,
    receiving_entity,
    paying_entity,
    product,
    contract_date,
    contract_total,
    intermediary_fee,
    previous_year_payment,
    current_year_payment,
    total_payment,
    outstanding_amount,
    invoice_amount,
    invoiced_unpaid_amount,
    payment_ratio,
    tax_freight,
    expected_remaining_payment,
    remark,
    monthly_amounts
)
```

---

# 第八部分：第一版开发边界

## 42. 第一版必须实现

- 分页读取飞书两张明细表全部记录；
- 解析飞书字段值；
- 签约按组别和人员汇总；
- 回款按人员汇总；
- 计算月度、季度和年度结果；
- 按参考 Excel 形式生成两个工作表；
- 生成个人、小组和部门汇总；
- 输出处理报告；
- 校验无 `#REF!` 等错误；
- 保留原始模板文件。

## 43. 第一版暂不实现

- 自动修改飞书原始数据；
- 自动回写汇总结果到飞书；
- 人员别名智能匹配；
- 多年度动态切换；
- 多币种换算；
- 图表和排行榜；
- 审批流；
- 数据库持久化。

---

# 第九部分：待业务确认事项

在正式编码前需要确认以下问题：

1. “签约个人汇总表”是否应正式更名为“回款明细表”？
2. 回款表是否需要在飞书中补充：
   - 合同日期；
   - 已开发票金额；
   - 已开票未回款；
   - 税/运费；
   - 备注。
3. 签约金额全年为 0 的跟进记录是否继续展示？
4. 新组别的排序是按首次出现，还是按固定业务顺序？
5. 金额为 0 时 Excel 显示 `0.00` 还是 `-`？
6. 回款待回款额为负数时是否保留负数？
7. 输出 Excel 是否需要保留公式，还是只保留 Python 计算值？
8. 回款可选列是否需要隐藏？
9. 是否需要增加独立的“数据异常”工作表？

在业务方未确认前，本文档采用以下默认值：

```text
全年金额为0的签约明细：保留
新组别排序：首次出现顺序
金额为0的展示：-
负待回款额：保留
Excel汇总：写入Python计算值
可选列：不隐藏
异常记录：写入processing_report.json，不新增工作表
```

---

# 44. 完成标准

生成的 Excel 同时满足以下条件，才视为汇总成功：

- 两个工作表名称正确；
- 表头和列结构正确；
- 签约明细全部按组别和人员展示；
- 回款明细全部按人员展示；
- 个人、小组、部门汇总结果正确；
- 合并单元格结构正确；
- 金额和比例格式正确；
- 无重复明细；
- 无 `#REF!`、`#DIV/0!`、`#VALUE!`；
- 输出文件不覆盖模板；
- 处理报告可定位所有异常记录；
- 汇总结果通过样例验算。
