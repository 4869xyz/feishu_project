# 飞书机器人完整对话指令规范

## 一、私聊与群聊格式

私聊直接发送命令：

```
云表列表
```

群聊必须直接 `@机器人`：

```
@机器人 云表列表
```

注意：

- 只 `@所有人` 不会触发机器人。
- 群聊中的链接和命令必须与 `@机器人` 放在同一条消息中。
- 建议严格按照以下命令文字发送，不要添加无关说明。

## 二、全部指令

| 指令         | 标准格式          | 作用                           |
| ------------ | ----------------- | ------------------------------ |
| 添加云表     | `添加云表 <链接>` | 长期登记一个或多个固定云表     |
| 云表列表     | `云表列表`        | 查看固定云表、当前编号和顺序   |
| 云表排序     | `云表排序 3 1 2`  | 调整全部固定云表的汇总顺序     |
| 移除云表     | `移除云表 2`      | 移除指定编号的固定云表         |
| 清空云表     | `清空云表`        | 清除当前用户的全部固定云表     |
| 汇总状态     | `汇总状态`        | 查看固定云表和临时文件         |
| 汇总         | `汇总`            | 刷新来源并生成汇总 Excel       |
| 清空汇总     | `清空汇总`        | 清除临时上传文件，保留固定云表 |
| 清空下载缓存 | `清空下载缓存`    | 管理员清理非活动本地文件       |

目前没有单独的“帮助”“撤销”“删除某个临时文件”命令。

## 三、添加固定云表

### 添加一个云表

```
添加云表 https://xxx.feishu.cn/sheets/shtxxxxxxxx
```

Wiki 表格也支持：

```
添加云表 https://xxx.feishu.cn/wiki/wikxxxxxxxx
```

### 批量添加云表

推荐使用换行：

```
添加云表
https://xxx.feishu.cn/sheets/sht111111
https://xxx.feishu.cn/wiki/wik222222
https://xxx.feishu.cn/sheets/sht333333
```

也可以使用空格：

```
添加云表 https://xxx.feishu.cn/sheets/sht111111 https://xxx.feishu.cn/sheets/sht222222
```

批量添加规则：

- Sheets 和 Wiki 链接可以混合。
- 链接只能使用空格或换行分隔。
- 按链接出现顺序登记。
- 同一消息中的重复链接自动忽略。
- 已经登记的云表不会重复添加。
- 某个链接失败不会影响其他链接继续登记。
- 最终统一回复成功、已存在、失败和当前总数。
- 登记时会立即导出并检查销售表结构。

## 四、查看云表

```
云表列表
```

机器人会返回类似：

```
当前共登记 3 份固定云表：
1. 2026年销售数据-张三｜最近刷新 2026-07-21T10:00:00
2. 2026年销售数据-李四｜最近刷新 2026-07-21T10:01:00
3. 2026年销售数据-王五｜最近刷新 2026-07-21T10:02:00
```

编号规则：

- 编号始终是连续的 `1、2、3……`。
- 编号同时表示当前汇总顺序。
- 删除或排序后会自动重新编号。
- 内部复杂的 `cloud-...` ID 不再向用户展示。

## 五、调整云表顺序

例如当前有三份云表，要把第 3 份放到最前面：

```
云表排序 3 1 2
```

要求：

- 必须填写当前全部编号。
- 每个编号只能出现一次。
- 不能缺少编号或填写越界编号。
- 可以使用空格、英文逗号或中文逗号分隔。

正确示例：

```
云表排序 3 1 2
云表排序 3,1,2
云表排序 3，1，2
```

错误示例：

```
云表排序 3 1
云表排序 3 3 1
云表排序 4 1 2
```

## 六、移除云表

先发送：

```
云表列表
```

取得当前编号后发送：

```
移除云表 2
```

执行后：

- 指定固定云表及其本地 latest 缓存被移除。
- 剩余云表自动重新编号。
- 不影响临时上传文件。
- 如需恢复，必须重新发送 `添加云表 <链接>`。

## 七、清空全部固定云表

```
清空云表
```

该命令会立即：

- 清除当前“会话 + 发送人”的全部固定云表。
- 删除对应的 latest 缓存。
- 保留临时上传文件。
- 不影响其他聊天或其他发送人的云表。

该命令没有二次确认，执行后需要重新添加链接才能恢复。

## 八、临时上传文件

### 直接上传 Excel

直接给机器人发送 `.xlsx` 文件，不需要命令。

机器人校验成功后会回复：

```
Excel 已加入当前汇总批次……
上传完成后发送“汇总”。
```

汇总业务目前只接受符合销售表结构的 `.xlsx`。请不要使用 `.xls` 或 `.xlsm` 作为汇总来源。

### 普通发送飞书链接

如果不加“添加云表”，链接只作为临时来源：

```
https://xxx.feishu.cn/sheets/shtxxxxxxxx
```

区别：

- `添加云表 <链接>`：长期保留，每次汇总自动刷新。
- 直接发送链接：只加入当前临时批次，成功汇总后清除。

普通临时链接一条消息只处理第一个有效链接；多个长期来源请使用批量“添加云表”。

## 九、查看汇总状态

```
汇总状态
```

机器人会分别显示：

- 固定云表及其顺序。
- 临时上传文件及其顺序。

实际汇总顺序始终是：

```
固定云表 → 临时文件
```

固定云表内部按照“云表列表”的数字顺序处理。

## 十、生成汇总

```
汇总
```

执行流程：

1. 按登记顺序重新下载所有固定云表的最新内容。
2. 重新验证每份固定云表。
3. 将固定云表放在临时文件之前。
4. 生成汇总 Excel。
5. 将结果文件回复到当前会话。

成功后：

- 临时汇总批次自动清空。
- 固定云表继续保留。
- 下次只需修改原飞书云表，再发送“汇总”。

失败时：

- 任一固定云表权限失效、链接失效、下载失败或结构错误，整个汇总中止。
- 不会使用旧缓存代替失败来源。
- 固定云表和临时文件都会保留，修正后可再次发送“汇总”。

## 十一、清空临时汇总

```
清空汇总
```

该命令只会：

- 清除当前用户的临时上传附件。
- 清除普通发送链接形成的临时来源。
- 保留所有固定云表。
- 不会立即删除本地文件实体。

`清空汇总` 与 `清空云表` 是两个不同命令：

```
清空汇总  → 只清临时来源
清空云表  → 只清固定云表
```

## 十二、管理员清理下载缓存

仅配置了管理员权限的用户可以发送：

```
清空下载缓存
```

清理范围：

- 非活动的附件下载文件。
- 非活动的链接归档文件。
- 历史汇总结果。

不会删除：

- 当前仍在汇总批次中的文件。
- 固定云表登记及 latest 缓存。
- 汇总模板。
- 批次状态文件。

普通用户执行会收到无权限提示。

## 十三、汇总文件处理规则

当前只生成签约数据汇总，回款汇总暂停处理。

输出规则包括：

- A:T 明细保留源表字体颜色。
- C:G 中任一有内容的字段为黄色或橙色时，整条明细自动隐藏。
- 空白字段残留黄色字体样式不会触发隐藏。
- 隐藏数据不会删除，仍参与个人、小组和部门合计。
- 汇总行和间隔空行保持可见。
- I:T 金额统一使用无货币符号、1 位小数的会计专用格式。
- I:T 根据各月份金额长度自动调整列宽。
- 回款表和其他非签约工作表保持原样。

## 十四、数据隔离规则

除管理员缓存清理外，所有操作均按照：

```
当前聊天 + 当前发送人
```

进行隔离。

因此在群聊中：

- 每个人只能查看和管理自己登记的云表。
- 每个人只能操作自己的临时汇总批次。
- 一个人发送“清空云表”不会清除其他人的云表。
- 不同群聊中的登记和批次互不影响。

## 十五、推荐完整使用流程

```
添加云表
https://xxx.feishu.cn/sheets/sht111111
https://xxx.feishu.cn/sheets/sht222222
https://xxx.feishu.cn/wiki/wik333333
```

然后查看：

```
云表列表
```

需要时调整顺序：

```
云表排序 3 1 2
```

检查当前来源：

```
汇总状态
```

生成结果：

```
汇总
```

以后更新飞书原云表后，只需再次发送：

```
汇总
```