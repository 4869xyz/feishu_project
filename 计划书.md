请在当前已有的 Python 项目中，实现“从飞书多维表格读取完整数据，并生成销售汇总 Excel”的功能。

开始编码之前，请先完整阅读当前仓库中的代码和以下三个文件：

1. `docs/飞书销售数据汇总规则_v1.0.md`
2. `templates/（实验）2026.07.03 2026年销售数据统计(1).xlsx`
3. `samples/feishu_connection_report.json`

其中：

- Markdown 文件是业务规则的唯一依据；
- Excel 文件是目标工作簿的表头、列宽、样式、合并单元格和展示结构参考；
- JSON 文件只用于了解飞书字段结构和开发测试，里面的 `sample_records` 不是完整业务数据；
- 正式运行时必须复用现有飞书 API 客户端，分页读取全部记录。

------

# 一、先检查现有项目

请先完成以下工作，不要立即重写代码：

1. 阅读当前项目目录。
2. 找到已有的飞书客户端。
3. 找到已有的配置读取模块。
4. 找到读取多维表格记录的方法。
5. 确认现有程序是否已经支持分页读取全部记录。
6. 列出现有模块中可以直接复用的类和函数。
7. 不要重复创建与现有功能相同的飞书连接模块。
8. 不要修改已经正常工作的鉴权逻辑，除非发现明确错误。
9. 不要删除已有环境变量。
10. 不要向飞书写入、修改或删除任何记录。

当前阶段飞书只作为只读数据源。

在真正修改代码前，先简要输出：

- 当前项目结构；
- 可复用模块；
- 计划新增或修改的文件；
- 每个文件的职责。

然后继续编码，不需要等待我确认。

------

# 二、项目目标

程序执行后完成以下流程：

```text
读取配置
    ↓
复用现有飞书客户端
    ↓
分页读取签约标准明细表全部记录
    ↓
分页读取回款明细表全部记录
    ↓
解析飞书字段值
    ↓
清洗和校验数据
    ↓
签约数据按组别、人员汇总
    ↓
回款数据按人员汇总
    ↓
复制 Excel 模板
    ↓
重建签约工作表数据区域
    ↓
重建回款工作表数据区域
    ↓
写入个人、小组、部门汇总
    ↓
检查金额和工作簿错误
    ↓
生成新的 Excel 文件
    ↓
生成 processing_report.json
```

------

# 三、数据表配置兼容

当前项目已有配置可能是：

```dotenv
STANDARD_DETAIL_TABLE_ID=
PERSON_SUMMARY_TABLE_ID=
```

目前实际情况是：

- `STANDARD_DETAIL_TABLE_ID` 对应签约标准明细；
- 当前名为 `PERSON_SUMMARY_TABLE_ID` 的表，实际字段属于回款明细。

请采用向后兼容方案：

1. 新增可选环境变量：

```dotenv
PAYMENT_DETAIL_TABLE_ID=
```

1. 如果 `PAYMENT_DETAIL_TABLE_ID` 已配置，优先使用它；
2. 如果未配置，则临时回退使用：

```text
PERSON_SUMMARY_TABLE_ID
```

1. 不要删除现有 `PERSON_SUMMARY_TABLE_ID`；
2. 在 README 中说明这是兼容旧配置的临时方案。

建议在配置类中暴露统一属性：

```python
payment_detail_table_id
```

但不要强制用户立即修改原配置。

------

# 四、运行入口

请新增独立脚本：

```text
scripts/generate_sales_report.py
```

支持运行：

```bash
python -m scripts.generate_sales_report
```

默认行为：

1. 从飞书读取全部数据；
2. 使用指定 Excel 模板；
3. 生成新的汇总 Excel；
4. 不覆盖原始模板；
5. 输出处理报告。

同时支持以下参数：

```bash
python -m scripts.generate_sales_report --limit 10
```

只读取或处理前 10 条记录，用于测试。

```bash
python -m scripts.generate_sales_report --input-json samples/feishu_connection_report.json
```

使用本地 JSON 样例测试，不访问飞书。

```bash
python -m scripts.generate_sales_report --template "templates/（实验）2026.07.03 2026年销售数据统计(1).xlsx"
```

指定模板路径。

```bash
python -m scripts.generate_sales_report --output-dir output
```

指定输出目录。

支持组合使用，例如：

```bash
python -m scripts.generate_sales_report \
  --input-json samples/feishu_connection_report.json \
  --template "templates/（实验）2026.07.03 2026年销售数据统计(1).xlsx" \
  --output-dir output
```

Windows PowerShell 中也必须可以正常运行。

------

# 五、代码模块设计

请优先适配当前项目结构。

如果项目中不存在对应模块，建议新增：

```text
models/
├── signing_record.py
└── payment_record.py

services/
├── feishu_record_parser.py
├── signing_transformer.py
├── payment_transformer.py
├── signing_aggregator.py
├── payment_aggregator.py
├── excel_report_builder.py
├── validation_service.py
└── processing_report_service.py

scripts/
└── generate_sales_report.py

tests/
├── test_feishu_record_parser.py
├── test_signing_aggregator.py
├── test_payment_aggregator.py
├── test_excel_report_builder.py
└── test_generate_sales_report.py
```

如果当前项目已有相同职责的模块，请复用或扩展，不要机械地重复创建文件。

------

# 六、飞书完整记录读取

正式模式必须调用现有飞书客户端读取完整记录。

签约数据表：

```text
STANDARD_DETAIL_TABLE_ID
```

回款数据表：

```text
PAYMENT_DETAIL_TABLE_ID
```

如果未配置，则回退：

```text
PERSON_SUMMARY_TABLE_ID
```

要求：

1. 分页读取全部记录；
2. 不能只读取前 3 条；
3. `max_records=None` 表示全部读取；
4. `--limit` 指定后才限制条数；
5. 每条记录保留：
   - `record_id`
   - `fields`
   - `created_time`
   - `last_modified_time`
6. 记录读取失败时给出表名、table_id 和飞书错误信息；
7. 不向飞书写入任何数据。

------

# 七、飞书字段解析

请根据 JSON 样例实现统一解析函数。

飞书文本字段常见格式：

```json
[
  {
    "text": "傲翔组",
    "type": "text"
  }
]
```

应解析为：

```text
傲翔组
```

需要支持：

1. 文本列表；
2. 普通字符串；
3. 数字；
4. `null`；
5. 空列表；
6. 缺失字段；
7. 多文本片段；
8. 文本数字。

建议实现：

```python
extract_text(value) -> str | None
extract_decimal(value, field_name, record_id) -> Decimal | None
extract_monthly_amounts(fields) -> dict[str, Decimal]
```

金额禁止使用 `float`，统一使用：

```python
Decimal
```

金额解析应支持：

```text
50000
"50000"
"50,000"
"￥50000"
"50000元"
"/"
"-"
"无"
空字符串
null
```

其中：

```text
"/"
"-"
"无"
空字符串
null
```

按 0 或空值处理，具体遵循汇总规则文件。

无法转换的金额不能静默忽略，必须加入异常报告。

------

# 八、签约数据处理

签约字段：

```text
组别
人员
客户简称
销售产品名称|项目名称
落地地点
客户来源（展会需注明）
备注
1月至12月
```

请创建签约记录模型，至少包含：

```python
record_id
group_name
person_name
customer_short_name
project_name
location
customer_source
remark
monthly_amounts
created_time
```

其中：

```python
monthly_amounts: dict[str, Decimal]
```

必须包含 1月至12月，缺失月份补 0。

签约有效记录至少必须有：

```text
组别
人员
```

缺少组别或人员：

- 不进入正式汇总；
- 写入失败记录；
- 保留 `record_id` 和失败原因。

全年金额均为 0 的签约记录默认保留。

------

# 九、签约汇总

签约汇总层级：

```text
组别
  人员
    明细
    个人月度合计
    个人季度合计
    个人年度合计
  小组月度合计
  小组季度合计
  小组年度合计
部门月度合计
部门季度合计
部门年度合计
```

月份列表：

```python
MONTH_FIELDS = [
    "1月", "2月", "3月", "4月", "5月", "6月",
    "7月", "8月", "9月", "10月", "11月", "12月"
]
```

季度：

```text
Q1 = 1月 + 2月 + 3月
Q2 = 4月 + 5月 + 6月
Q3 = 7月 + 8月 + 9月
Q4 = 10月 + 11月 + 12月
```

排序规则：

1. 如果代码配置了固定组别顺序，优先使用；
2. 其他新组别按第一次出现顺序追加；
3. 同组人员按第一次出现顺序；
4. 同一人员的明细按 `created_time` 升序；
5. 不同组别的同名人员不得合并。

序号规则：

- B 列由 Python 生成；
- 每个人员的明细从 1 开始；
- 汇总行和空行不编号。

------

# 十、回款数据处理

当前回款字段：

```text
姓名
收款主体
付款主体
产品
合同总价
居间费
已回款-提成口径（26年以前）
已回款-提成口径（26年）
已回款-提成口径总额
待回款额
回款比例
预计剩余回款时间及金额
1月至12月
```

请创建回款记录模型，至少包含：

```python
record_id
person_name
receiving_entity
paying_entity
product
contract_date
contract_total
intermediary_fee
previous_year_payment
current_year_payment
total_payment
outstanding_amount
payment_ratio
expected_remaining_payment
monthly_amounts
created_time
```

当前飞书表没有以下字段：

```text
合同日期
已开发票金额
已开票未回款
税/运费
备注
```

处理方式：

- Excel 中保留这些列；
- 值留空；
- 不猜测；
- 不把异常数据自动挪到其他字段。

回款有效记录至少必须有：

```text
姓名
```

缺少姓名的记录不进入正式汇总。

------

# 十一、回款派生字段

不能直接相信飞书中已有的汇总字段。

请根据月份和基础字段重新计算：

```text
26年回款 = 1月至12月之和
已回款总额 = 26年以前回款 + 26年回款
待回款额 = 合同总价 - 已回款总额
回款比例 = 已回款总额 / 合同总价
```

当合同总价为空或为 0：

```text
回款比例留空
```

不能出现：

```text
#DIV/0!
```

飞书中的以下原值只用于对比校验：

```text
已回款-提成口径（26年）
已回款-提成口径总额
待回款额
回款比例
```

当源值和 Python 计算值差异超过：

```text
0.01
```

将差异写入 `processing_report.json`。

最终 Excel 使用 Python 计算值。

------

# 十二、目标 Excel

模板文件：

```text
templates/（实验）2026.07.03 2026年销售数据统计(1).xlsx
```

输出文件名称：

```text
2026年销售数据统计_YYYYMMDD_HHMMSS.xlsx
```

输出目录默认：

```text
output/
```

禁止覆盖模板。

需要保留以下工作表：

```text
展示用-2026年签约数据汇总（0702统计）
展示用-2026年回款数据汇总(0702统计)
```

模板只用于复用：

- 工作表名称；
- 表头；
- 列宽；
- 行高；
- 颜色；
- 字体；
- 对齐；
- 边框；
- 合并规则风格。

不要复制模板旧动态数据和旧错误公式。

------

# 十三、签约 Excel 结构

签约表列范围：

```text
A:T
```

字段：

```text
A 组别
B 序号
C 人员
D 客户简称
E 销售产品名称/项目名称
F 落地地点
G 客户来源（展会需注明）
H 备注
I:T 1月至12月
```

表头：

- A2:A3 至 H2:H3 分别纵向合并；
- I2:T2 合并；
- I3:T3 显示 1月至12月；
- 数据从第 4 行开始。

每个人员结构：

```text
人员明细
个人月度合计
个人季度合计
个人年度合计
空一行
```

每个小组结束后：

```text
小组月度合计
小组季度合计
小组年度合计
空一行
```

最后：

```text
部门月度合计
部门季度合计
部门年度合计
```

合并规则必须严格遵守 Markdown 规则文件。

------

# 十四、回款 Excel 结构

回款表列范围：

```text
A:AC
```

字段映射和合并规则以：

```text
docs/飞书销售数据汇总规则_v1.0.md
```

为准。

每个人员：

```text
人员明细
个人小计
空一行
```

最后：

```text
部门总计
```

个人和部门回款比例必须使用：

```text
已回款总额 / 合同总价
```

不能直接加总比例，也不能对比例做简单平均。

------

# 十五、Excel 写入方式

第一版采用：

```text
Python 计算数值，Excel 写入最终数值
```

不要在动态数据区域依赖旧 Excel 公式。

原因：

- 避免 `#REF!`；
- 避免插入行后公式错位；
- 避免 Excel 未重新计算；
- 便于测试和校验。

要求：

1. 使用 `openpyxl` 处理模板；
2. 清理旧动态区域；
3. 清理旧动态区域的合并单元格；
4. 保留表头；
5. 重新生成所有明细和汇总；
6. 重新创建合并单元格；
7. 写入 Python 计算结果；
8. 应用模板样式；
9. 保存到新文件。

------

# 十六、处理报告

输出 Excel 的同时生成：

```text
output/processing_report_YYYYMMDD_HHMMSS.json
```

报告至少包含：

```json
{
  "run_time": "",
  "source_mode": "feishu 或 json",
  "output_excel": "",
  "signing": {
    "input_count": 0,
    "valid_count": 0,
    "failed_count": 0,
    "group_count": 0,
    "person_count": 0,
    "annual_total": "0"
  },
  "payment": {
    "input_count": 0,
    "valid_count": 0,
    "failed_count": 0,
    "person_count": 0,
    "contract_total": "0",
    "payment_total": "0"
  },
  "warnings": [],
  "failed_records": []
}
```

`Decimal` 写入 JSON 时转换为字符串，避免精度丢失。

失败记录至少包含：

```text
table_name
record_id
field_name
raw_value
error_reason
```

------

# 十七、错误扫描和校验

生成 Excel 后必须扫描整个工作簿。

不能存在：

```text
#REF!
#DIV/0!
#VALUE!
#NAME?
#N/A
```

还要验证：

签约：

```text
个人年度合计 = 个人12个月之和
小组年度合计 = 小组所有人员年度合计
部门年度合计 = 所有小组年度合计
部门年度合计 = 所有有效明细金额之和
```

回款：

```text
26年回款 = 12个月金额之和
回款总额 = 26年以前回款 + 26年回款
待回款额 = 合同总价 - 回款总额
回款比例 = 回款总额 / 合同总价
个人小计 = 个人全部明细之和
部门总计 = 全部有效明细之和
```

校验失败时：

- 不要删除已生成的文件；
- 在报告中标记失败；
- 命令行返回非 0 退出码；
- 清晰打印失败原因。

------

# 十八、样例验收数据

请使用 `samples/feishu_connection_report.json` 编写单元测试。

签约样例：

## 李一

```text
1月 = 5000
3月 = 60000
Q1 = 65000
年度合计 = 65000
```

## 李四

两条记录汇总：

```text
1月 = 60000
5月 = 6000
7月 = 50000
12月 = 6000
Q1 = 60000
Q2 = 6000
Q3 = 50000
Q4 = 6000
年度合计 = 122000
```

回款样例：

## 林雪

```text
1月 = 5000
2月 = 600
4月 = 1000
26年回款 = 6600
合同总价 = 500000
26年以前回款 = 0
已回款总额 = 6600
待回款额 = 493400
回款比例 = 0.0132
```

## 吴傲翔

```text
合同总价 = 2026/02/01
```

必须标记为合同总价格式异常。

月份：

```text
2月 = 1
7月 = 18700
26年回款 = 18701
```

如果源汇总值为 18700，应记录差异。

这些测试必须通过。

------

# 十九、测试要求

请使用 pytest。

至少测试：

1. 飞书文本列表解析；
2. 文本数字转换；
3. 空值和 `/` 的处理；
4. 缺失月份补 0；
5. 非法金额记录异常；
6. 签约个人汇总；
7. 签约小组汇总；
8. 签约部门汇总；
9. 回款派生字段；
10. 回款个人小计；
11. 回款部门总计；
12. Excel 文件成功生成；
13. 两个工作表存在；
14. 模板未被覆盖；
15. 关键合并单元格存在；
16. 工作簿没有错误值；
17. JSON 样例模式不访问真实飞书；
18. 正式模式调用现有飞书客户端；
19. `--limit` 生效。

运行：

```bash
pytest
```

所有测试必须通过。

------

# 二十、依赖

优先复用当前依赖。

如果尚未安装，请在 `requirements.txt` 中增加：

```text
openpyxl
```

只有确实需要时才增加：

```text
pandas
```

由于本项目涉及合并单元格、模板样式和复杂格式，Excel 生成核心必须使用：

```text
openpyxl
```

不要引入数据库、Web 框架或当前阶段不需要的库。

------

# 二十一、README

更新 README，增加：

1. 功能说明；
2. 文件目录说明；
3. 模板放置位置；
4. 汇总规则文件位置；
5. 从飞书生成报告的命令；
6. 从本地 JSON 测试的命令；
7. `--limit` 用法；
8. 输出文件位置；
9. 当前只读飞书、不回写飞书；
10. `PAYMENT_DETAIL_TABLE_ID` 的兼容规则；
11. 常见错误说明。

------

# 二十二、代码质量要求

1. Python 3.11+；
2. 完整类型注解；
3. 关键类和函数添加 docstring；
4. 金额统一使用 Decimal；
5. 主脚本只负责编排；
6. 解析、汇总、Excel 构建、校验职责分离；
7. 不在函数内部硬编码本机绝对路径；
8. 不打印 app_secret；
9. 不打印完整 tenant_access_token；
10. 不修改飞书源数据；
11. 不覆盖 Excel 模板；
12. 不吞掉异常；
13. 错误必须包含 record_id 和字段名；
14. 中文文件名和内容使用 UTF-8；
15. 保持与现有项目代码风格一致。

------

# 二十三、开发顺序

请按照以下顺序实施，并在每一步后运行对应测试：

1. 检查和复用现有项目；
2. 添加配置兼容；
3. 实现飞书字段解析；
4. 创建签约和回款数据模型；
5. 实现签约转换；
6. 实现签约汇总；
7. 实现回款转换和派生字段；
8. 实现回款汇总；
9. 实现 Excel 模板重建；
10. 实现处理报告；
11. 实现结果校验；
12. 实现命令行脚本；
13. 添加单元测试；
14. 使用本地 JSON 生成测试 Excel；
15. 运行完整 pytest；
16. 更新 README。

不要在测试尚未通过时扩展其他功能。

------

# 二十四、完成后必须汇报

完成后，请输出：

1. 新增文件列表；
2. 修改文件列表；
3. 每个文件职责；
4. 现有飞书模块复用了哪些部分；
5. 是否修改了环境变量；
6. 安装依赖命令；
7. 本地 JSON 测试命令；
8. 正式飞书运行命令；
9. pytest 结果；
10. 生成的 Excel 路径；
11. 生成的处理报告路径；
12. 样例验算结果；
13. 发现的数据异常；
14. 尚未完成的功能；
15. 需要我人工确认的业务问题。

如果因为缺少真实完整飞书数据而无法完成正式数据验收，请明确说明，但仍然必须完成本地 JSON 模式、单元测试和 Excel 生成测试。

不要声称真实飞书汇总已经成功，除非实际运行并获得成功结果。